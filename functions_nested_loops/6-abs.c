#include "main.h"

/**
 * _abs - Function that computes the absolute value of an integer.
 * @n: Presenting an integer.
 * Return: -n
 */

int _abs(int n)

{

	while (n >= 0)
		{
		return (n);

		}

	return (-n);
}
